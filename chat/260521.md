# BMad PRD 创建会话记录 — 2026-05-21

> 续 260520.md，PRD工作流 Step 4

---

## PRD 创建 — Step 4：User Journey Mapping

### 用户类型识别

从之前的讨论和头脑风暴文档中，识别出以下用户类型：

1. **数字后端工程师** — 日常探索、版本提交、问题追溯
2. **PD Lead / Committer** — 交付把控、流水线触发、版本晋升
3. **IT基础设施工程师** — 系统部署、存储监控、运维管理

用户确认三个角色都是关键角色。

---

### Journey 1：工程师的日常探索 — 从混沌到有序

**角色：** 李明，数字后端工程师，5年经验

**场景：** 3DIC项目floorplan阶段，需要尝试三种不同布局策略，对比PPA结果。

**过去的痛点：** 在NAS上创建三个手动命名的目录（`fp_v1_baseline`等），跑完后记不清哪个目录用了什么参数，哪个结果对应哪个输入。

**BIG下的工作流：**

```bash
big checkout feature/fp-exploration

# 第一次尝试：baseline布局
big commit -m "baseline floorplan" --inputs "rtl/*.v, sdc/*.sdc" \
  --params "place_density=0.6, util=0.7" --outputs "out/*.def, out/*.gds"
# → 制品集 v1，Exploring状态

# 第二次尝试：channel routing策略
big commit -m "channel routing floorplan" --inputs "rtl/*.v, sdc/*.sdc" \
  --params "place_density=0.55, util=0.65, channel_width=2um" --outputs "out/*.def, out/*.gds"
# → 制品集 v2，输入哈希相同但配方不同

# 第三次尝试：shared power策略
big commit -m "shared power floorplan" --inputs "rtl/*.v, sdc/*.sdc" \
  --params "place_density=0.6, util=0.7, power_shared=true" --outputs "out/*.def, out/*.gds"
# → 制品集 v3

# 查看三次探索
big log --oneline --branch feature/fp-exploration
```

**揭示的能力需求：** 制品集commit与配方记录、输入文件完整性自动校验、Exploring状态的轻量存储、分支版本历史浏览

---

### Journey 2：问题追溯与回退 — 从半天到30秒

**角色：** 王磊，数字后端工程师，3年经验

**场景：** 3DIC项目signoff时序分析发现关键路径setup slack为负值，需要定位引入问题的版本。

**过去的痛点：** 翻阅多个目录，对照命名规则猜测文件先后关系，打开工具逐个检查参数，可能还要问同事，整个过程至少半天。

**BIG下的工作流：**

```bash
# 从问题制品集出发，查看完整血缘链
big log --lineage signoff_v5
# signoff_v5 [Golden candidate]
#   ← route_v8 [Pinned] (路由结果)
#     ← place_v12 [Pinned] (布局结果)
#       ← fp_v3 [Candidate] (floorplan，shared power策略)
#         ← synth_v2 [Candidate] (综合结果)
#           ← rtl_v7 [Pinned] (RTL提交)
# 参数变更链：fp_v3: power_shared=true ← 唯一引入该参数的版本

# 30秒内锁定问题源头：fp_v3的shared power策略导致时序收敛困难
# 决定回退到floorplan阶段重新做布局

big checkout fp_v2
# 在fp_v2（channel routing策略）基础上重新做floorplan探索
# ... 调整布局参数，重新跑工具 ...
big commit -m "revised floorplan based on channel routing" \
  --inputs "rtl/*.v, sdc/*.sdc" \
  --params "place_density=0.55, util=0.65, channel_width=2.5um" \
  --outputs "out/*.def, out/*.gds"
# → 新制品集，版本图上出现回退边（环）
```

**用户纠正：** AI最初写的是"回退到上游RTL阶段修改代码"，用户指出应该回退到floorplan阶段重新做布局，因为问题出在fp_v3的布局策略，而不是RTL代码。

**揭示的能力需求：** 血缘链双向追溯、版本图环形结构支持（回退边）、基于历史版本的checkout与分支、制品集参数对比与差异展示

---

### Journey 3：PD Lead的交付把控 — 流水线驱动的规范化

**角色：** 张薇，PD Lead（Committer），10年经验

**场景：** 前端团队提交新RTL版本，需要走完整流水线交付到后端。

**过去的痛点：** 上次tapeout前夕，交付的GDS混用了两个不同版本的DEF，导致LVS验证失败，后端团队加班三天修复。根因是工程师手动拷贝文件时选错了输入版本。

**BIG下的工作流：**

```bash
# 前端工程师提交RTL
big commit -m "RTL v8: fix timing violation on path A" \
  --inputs "*.v" --outputs "*.v"
# → rtl_v8 生成，自动标记为 Pinned

# 张薇触发交付流水线
big pipeline run rtl-to-backend --from rtl_v8
# 流水线自动执行：
# [1/6] ✅ RTL完整性校验（清单核对，无遗漏文件）
# [2/6] ⏳ 综合执行中...（约4小时）
#        → synth_v8 生成，配方 hash(rtl_v8 + 综合参数) 自动记录
#        → 检测到配方匹配历史输出 synth_v5，跳过综合！节省4小时
# [3/6] ⏳ LVS验证执行中...
#        → 调用验证工具，自动使用 synth_v8 的输出
#        → LVS clean ✅
# [4/6] ✅ 前端出口检查（时序、面积、功耗指标采集）
# [5/6] ✅ 后端入口检查（DEF一致性、约束文件完整性）
# [6/6] ✅ 发布到交付制品系统

big pipeline status rtl-to-backend
# Pipeline rtl-to-backend: COMPLETED (6/6)
# Released: deliverable_v3 [Golden]
```

**揭示的能力需求：** 流水线定义与编排、流水线触发与自动串行执行、验证门禁（LVS/DRC自动调用）、配方即缓存Key（检测已有输出复用）、指标采集与版本绑定、阶段晋升（Pinned → Golden）、与外部交付制品系统集成

---

### Journey 4：IT管理员的系统运维 — 部署与监控

**角色：** 陈浩，IT基础设施工程师

**场景：** 在NAS上部署BIG版本仓库，配置存储策略，监控使用情况。

**BIG下的工作流：**

```bash
# 在NAS共享目录上初始化BIG版本仓库
big repo init /nas/projects/3dic-project-2026
# → 创建 .big/ 元数据目录
# → 初始化CAS存储池（指向NAS路径）
# → 生成默认配置文件 .big/config.toml

# 配置分层存储策略
big repo config set storage.golden.replicas 2
big repo config set storage.golden.paths ["/nas/golden-backup-1", "/nas/golden-backup-2"]

# 创建团队分支
big branch create feature/fp-exploration --owner liming
big branch create feature/pd-delivery --owner zhangwei

# 监控存储使用
big repo stats
# Storage: 2.3TB used / 12TB available (19%)
# CAS chunks: 1,247,832 | Dedup ratio: 8.7x
# Exploring: 894 artifact sets (0.3TB recipes only)
# Candidate: 127 artifact sets (1.8TB)
# Pinned: 43 artifact sets (0.2TB)
# Golden: 12 artifact sets (0.0TB, replicated to 2 locations)

# GC回收被淘汰分支的存储
big repo gc --dry-run
# Would reclaim: 340GB from 2,156 orphaned chunks
big repo gc
# Reclaimed: 340GB
```

**用户纠正：** AI最初使用的命令是 `big server init`，用户认为 `big repo init` 更好。

**揭示的能力需求：** NAS原生部署、分层存储策略配置、存储统计与监控、CAS块级去重与GC回收、Golden数据冗余备份、分支管理与权限

---

### Journey Requirements Summary

| 能力领域 | 揭示的旅程 | 关键需求 |
|---------|-----------|---------|
| 制品集版本管理 | J1, J2, J3 | commit/checkout/log、配方记录、输入校验、环形版本图 |
| 血缘追溯 | J2 | 双向追溯、参数对比、决策链可视化 |
| 分层存储 | J1, J4 | Exploring轻量存储、Candidate完整存储、Golden冗余备份 |
| 流水线引擎 | J3 | 步骤编排、自动执行、验证门禁、指标采集、阶段晋升 |
| 配方缓存 | J3 | hash(输入+参数)匹配、输出复用、跳过重复计算 |
| NAS兼容 | J4 | 原生文件系统部署、一分支一目录、CAS增量拼接 |
| 系统运维 | J4 | 存储统计、GC回收、分层策略配置、监控 |

---

## Step 5：Domain-Specific Requirements（进行中）

### 初始领域约束分析

半导体/EDA领域复杂度高，CSV中无直接映射，但与aerospace有相似性——tapeout不可逆、数据完整性要求极高。

AI初步归纳的领域关切方向：

1. **不可逆性约束** — Tapeout不可逆，Golden数据完整性和审计追溯至关重要
2. **EDA工具链兼容** — BIG必须对EDA工具透明
3. **IP安全与访问控制** — 芯片设计数据是核心IP
4. **I/O性能约束** — EDA工作I/O密集，版本管理层不能显著影响吞吐
5. **NAS基础设施约束** — 必须兼容主流NAS文件系统

**待讨论：** 用户尚未回复领域约束的补充和确认，下次会话继续。

---

## 下一步

PRD Step 5 继续：Domain-Specific Requirements 讨论
